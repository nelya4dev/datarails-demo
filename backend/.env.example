# DB
DB_HOST=postgres
DB_PORT=5432
DB_NAME=demo
DB_USER=postgres
DB_PASSWORD=123456

# PGADMIN (for database management UI)
PGADMIN_DEFAULT_EMAIL=nelya4dev@gmail.com
PGADMIN_DEFAULT_PASSWORD=123456789

# CORS (Controls which browser apps can call the API)
CORS_ALLOW_ALL_ORIGINS=true
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8001

# SECURITY (Protects against Host header injection attacks)
# Development: Leave empty (defaults to "*" = allow all hosts)
# Production: Set to your actual domain(s)
ALLOWED_HOSTS=

# APPLICATION
APP_MODE=development
UPLOAD_DIR=uploads
UPLOAD_MAX_FILE_SIZE=10485760

CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1
CONFIG_CSV_PATH=config/config.csv